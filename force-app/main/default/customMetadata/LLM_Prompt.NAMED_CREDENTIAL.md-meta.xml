<?xml version="1.0" encoding="UTF-8" ?>
<CustomMetadata
  xmlns="http://soap.sforce.com/2006/04/metadata"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xmlns:xsd="http://www.w3.org/2001/XMLSchema"
>
    <label>Named Credential</label>
    <protected>false</protected>
    <values>
        <field>Feature_Key__c</field>
        <value xsi:type="xsd:string">NAMED_CREDENTIAL</value>
    </values>
    <values>
        <field>Prompt_Content__c</field>
        <value
      xsi:type="xsd:string"
    ><![CDATA[
== NAMED CREDENTIAL XML FORMAT ==
File path: namedCredentials/ApiName.namedCredential
metadataType: NamedCredential
memberName: ApiName (no __c suffix)

Named Credential has TWO distinct XML shapes depending on which model the ticket wants.
Do NOT mix elements from one shape into the other — Salesforce rejects the file outright.

== WHICH MODEL? ==
- LEGACY (single-object, "PATH A" below): ticket says "No Authentication", "Password
  Authentication", "Basic Auth", or "OAuth 2.0" with no mention of an External Credential.
  Auth lives directly on the NamedCredential (protocol, username, password, authProvider).
- MODERN (External-Credential-based, "PATH B" below): ticket explicitly says "External
  Credential", "modern"/"enhanced" Named Credential, or needs AWS Signature / Custom
  protocol / Per-User Principal — none of which the legacy model supports. Auth lives on a
  SEPARATE ExternalCredential component (see the EXTERNAL_CREDENTIAL prompt); this
  NamedCredential file only holds the URL and a reference to that ExternalCredential.
  A ticket asking to create both is TWO files: one ExternalCredential + one NamedCredential.

== PATH A — LEGACY (single-object) ==
IDENTITY TYPE is always Named Principal → principalType = NamedUser
Pick the correct protocol template based on what the ticket specifies.

CRITICAL — ELEMENT ORDER: elements MUST appear in strict alphabetical order.
Salesforce rejects Named Credential XML with wrong element order.
Do NOT include <fullName> inside the XML body — the file name is the identity.

-- PROTOCOL: No Authentication --
Use when ticket says: "No Authentication", "No Auth", "anonymous"
<?xml version="1.0" encoding="UTF-8"?>
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <calloutStatus>Enabled</calloutStatus>
    <endpoint>https://api.example.com</endpoint>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
    <label>Label</label>
    <principalType>NamedUser</principalType>
    <protocol>NoAuthentication</protocol>
</NamedCredential>

-- PROTOCOL: OAuth 2.0 --
Use when ticket says: "OAuth 2.0", "OAuth"
Required from ticket: Authentication Provider name + Scope
<?xml version="1.0" encoding="UTF-8"?>
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <authProvider>AuthProviderDeveloperName</authProvider>
    <calloutStatus>Enabled</calloutStatus>
    <endpoint>https://api.example.com</endpoint>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
    <label>Label</label>
    <oauthScope>api refresh_token</oauthScope>
    <principalType>NamedUser</principalType>
    <protocol>Oauth</protocol>
</NamedCredential>

-- PROTOCOL: Password Authentication --
Use when ticket says: "Password Authentication", "Password Auth", "Basic Auth"
Required from ticket: Username + Password
<?xml version="1.0" encoding="UTF-8"?>
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <calloutStatus>Enabled</calloutStatus>
    <endpoint>https://api.example.com</endpoint>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
    <label>Label</label>
    <password>thepassword</password>
    <principalType>NamedUser</principalType>
    <protocol>Password</protocol>
    <username>user@example.com</username>
</NamedCredential>

RULES:
- principalType is ALWAYS NamedUser — never Anonymous
- calloutStatus is ALWAYS Enabled
- generateAuthorizationHeader is ALWAYS true
- allowMergeFieldsInBody and allowMergeFieldsInHeader default to false unless ticket says otherwise
- Element order MUST be strictly alphabetical — any deviation causes deploy failure
- Do NOT include <fullName> inside the XML body
- For OAuth 2.0: authProvider value = the DeveloperName of the Auth Provider stated in the ticket
- For OAuth 2.0: oauthScope value = scope string stated in the ticket (e.g. "api refresh_token")
- For Password: username and password come directly from the ticket
- NEVER invent or guess authProvider, username, or password — use only what the ticket provides
- Each Named Credential is its own separate file — do NOT merge into one file

== PATH B — MODERN (External-Credential-based) ==
CRITICAL — ELEMENT ORDER here is DIFFERENT from PATH A and is NOT alphabetical.
Follow the order shown in this template exactly: label, namedCredentialType,
namedCredentialParameters (repeated, in the order given), then the boolean flags.

<?xml version="1.0" encoding="UTF-8"?>
<NamedCredential xmlns="http://soap.sforce.com/2006/04/metadata">
    <label>Test Analytics API</label>
    <namedCredentialType>SecuredEndpoint</namedCredentialType>
    <namedCredentialParameters>
        <parameterName>DefaultEndpoint</parameterName>
        <parameterType>Url</parameterType>
        <parameterValue>https://api.analytics-example.com</parameterValue>
    </namedCredentialParameters>
    <namedCredentialParameters>
        <parameterName>DefaultAuth</parameterName>
        <parameterType>Authentication</parameterType>
        <externalCredential>Test_Analytics_API_Credential</externalCredential>
    </namedCredentialParameters>
    <allowMergeFieldsInBody>false</allowMergeFieldsInBody>
    <allowMergeFieldsInHeader>false</allowMergeFieldsInHeader>
    <generateAuthorizationHeader>true</generateAuthorizationHeader>
</NamedCredential>

namedCredentialType valid values: SecuredEndpoint (standard, most common), PrivateEndpoint
(when ticket mentions a private connection / outbound network connection), Legacy, Standard.

namedCredentialParameters.parameterType valid values: Url, Authentication, ClientCertificate,
OutboundNetworkConnection, HttpHeader, AllowedManagedPackageNamespaces, ManagedByNamespace.
- parameterType=Url → parameterValue holds the endpoint URL (this replaces PATH A's <endpoint>)
- parameterType=Authentication → externalCredential holds the ExternalCredential's apiName
  (this is how the two components link together — NOT a top-level <externalCredential> field)
- parameterType=ClientCertificate → certificate holds the certificate name, only if ticket asks for it

RULES FOR PATH B:
- Always include exactly one Url parameter and exactly one Authentication parameter at minimum.
- The externalCredential value MUST match an ExternalCredential apiName that exists in orgState
  or is being created in the SAME ticket via the EXTERNAL_CREDENTIAL prompt's format — never invent one.
- Do NOT include <protocol>, <principalType>, <username>, <password>, <authProvider>, or
  <endpoint> in a PATH B file — those are PATH A (legacy) elements and are invalid here.
- A ticket asking to "create a Named Credential referencing an External Credential" is ALWAYS
  PATH B — do not fall back to PATH A even if no explicit protocol keyword is mentioned.

-- UPDATE --
The current state of all Named Credentials is in orgState.existingNamedCredentials.
Each entry contains: apiName, label, endpoint, protocol, principalType, calloutStatus,
generateAuthorizationHeader, allowMergeFieldsInBody, allowMergeFieldsInHeader,
and protocol-specific fields: authProvider + oauthScope (OAuth), username (Password).

To UPDATE a Named Credential:
1. Find the NC in existingNamedCredentials by apiName
2. If NOT found in orgState → return an error in summary, do NOT generate any files
3. Regenerate the COMPLETE XML using ALL existing values from orgState
4. Only change the specific field(s) the ticket requests
5. Preserve protocol — do NOT change protocol unless ticket explicitly requests it
6. For Password protocol: username comes from orgState (already known).
   password is NEVER returned by any API — it MUST come from the ticket.
   If the ticket does not provide a password → return error in summary, do NOT deploy
7. Use metadataType=NamedCredential, memberName=apiName, filePath=namedCredentials/{apiName}.namedCredential

-- DELETE --
To DELETE a Named Credential:
1. Confirm the apiName exists in existingNamedCredentials in orgState
2. If NOT found → return error in summary, do NOT generate destructiveFiles
3. Use the shorthand destructiveFiles entry (no xmlContent needed — Apex builds it):
   {
     "metadataType": "NamedCredential",
     "member": "My_API"
   }
4. Do NOT generate any files[] entry for delete
5. Do NOT generate xmlContent — Apex builds destructiveChangesPost.xml automatically]]></value>
    </values>
    <values>
        <field>Sort_Order__c</field>
        <value xsi:type="xsd:double">80.0</value>
    </values>
    <values>
        <field>Is_Always_Included__c</field>
        <value xsi:type="xsd:boolean">false</value>
    </values>
    <values>
        <field>Is_Active__c</field>
        <value xsi:type="xsd:boolean">true</value>
    </values>
    <values>
        <field>Depends_On__c</field>
        <value xsi:type="xsd:string">EXTERNAL_CREDENTIAL</value>
    </values>
    <values>
        <field>Description__c</field>
        <value
      xsi:type="xsd:string"
    >Named Credential XML format — legacy single-object model (No Auth, OAuth2, Password) and modern External-Credential-referencing model</value>
    </values>
</CustomMetadata>
